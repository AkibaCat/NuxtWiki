## 1.3.1 版本说明

### 功能更新：

- 加入内容自定义样式(scss)
- 后台加入在线更新版本功能

### 语法调整：

- 语法`[{文本|颜色}]`调整为 -> `[文本]{颜色*}`，`{颜色*}` 可填入颜色代码与样式类名

### 页面编辑器更新：

- 加入样式编辑器，支持语法高亮与代码补全
- 编辑器字体调整（Consolas, 'Courier New', monospace）
